{
  "name": "func-sb-demo",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "prestart": "npm run build",
    "start": "func start --verbose"
  },
  "dependencies": {
    "@azure/service-bus": "^7.10.0",
    "@azure/functions": "^3.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^18.0.0"
  }
}